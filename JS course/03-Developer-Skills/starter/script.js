// Remember, we're gonna use strict mode in all scripts now!
"use strict";

const user = {
  name: "Daniel",
  age: 24,
  occupation: "student",
};

const arr = [1, "hello", "world"];

// console.log(Object.keys(user));

// for (let i = 0; i < user.length; i++) {
//   console.log(`This is ${user}`)
// }
