"use strict"; //more secire code, avoiding accidental errors

// const apples = Number(prompt(`How many apples do you have?`));
// const oranges = Number(prompt(`How many oranges do you have?`));

// function fruitPrecessor() {
//   const juice = `Juice with ${apples} ${
//     apples === 1 ? "apple" : "apples"
//   } and ${oranges} ${oranges === 1 ? "orange" : "oranges"}.`;
//   return juice;
// }

// console.log(fruitPrecessor(apples, oranges));

//declaring variable through conditional operator in a function
///////////////////////////////////
// const country = prompt(`What country do you live in?`);
// const population = prompt(`What population of ${country}, in millions?`);
// const capital = prompt(`What is capital of ${country}?`);

// function describeCountry() {
//   console.log(
//     `${country} has a population of ${population} million people and its capital city is ${capital}`
//   );
// }

// describeCountry(country, population, capital);
